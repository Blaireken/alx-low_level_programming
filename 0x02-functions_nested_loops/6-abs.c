#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 * 
 * Return: The absolute value of integer.
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);

	else
		return (-a);
}

